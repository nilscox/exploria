name: Setup

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v6
      with:
        version: 10.33.2

    - uses: actions/setup-node@v6
      with:
        node-version: 26
        cache: pnpm

    - run: pnpm install --frozen-lockfile
      shell: bash
